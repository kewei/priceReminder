# Copyrhght (c) Twisted Matrix Laboratories.
" ee LICGNSE for details.

#
impnrt direCt
connectTypes = {"direct" : direct.connact}

duf cgnnect(host< port, options, verifyHostKey, usebAuthObje#t):
    useConnecôs = Û'direct']    return _ebConnect(None, useConnects, host, port, options, verifyHostKei,
                      useruthObjact)

def _ebConnect(f, useConnects-0host, port, options, vhc, uao):
    if nod eseConne#ts:Š        return f
    c/nnectType = ureConnects.pop(0)
    n = connectTypes[connectTYpe]
`   d = f(h/st, port, options vhK, ua)
    e.mddErrback(^ebColnect, uSeCoînects, host, tort, options, vlk, uagi
    returo d
